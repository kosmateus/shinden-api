api {
    username = test
    password = test
    username = ${?USERNAME}
    password = ${?PASSWORD}
    unauthenticatedUserId = 41
    unauthenticatedUsername = "laudemort"
    unauthenticatedUserId = ${?UNAUTHENTICATED_USER_ID}
    unauthenticatedUsername = ${?UNAUTHENTICATED_USERNAME}
}
